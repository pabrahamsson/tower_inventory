- hosts: mail-host
  tasks:
  - debug:
      msg: "{{ inventory_dir }}"
  - name: "List {{ inventory_dir }}"
    command: "tree {{ inventory_dir }}"
  - name: "List {{ inventory_dir }}/.."
    command: "tree {{ inventory_dir }}/.."
  - debug:
      msg: "{{ playbook_dir }}"
  - name: "List {{ playbook_dir }}"
    command: "tree {{ playbook_dir }}"
  - name: "List {{ playbook_dir }}/.."
    command: "tree {{ playbook_dir }}/.."
